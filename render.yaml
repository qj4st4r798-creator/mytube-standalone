services:
  - type: web
    name: northstar-math-academy
    env: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    plan: starter
    disk:
      name: northstar-storage
      mountPath: /opt/render/project/src/uploads
      sizeGB: 10
