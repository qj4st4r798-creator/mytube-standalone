{
  "name": "mytube-standalone",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone MyTube website with local auth, uploads, moderation, live camera broadcasting, and stock quotes.",
  "dependencies": {
    "busboy": "^1.6.0",
    "cookie": "^0.7.2",
    "sql.js": "^1.13.0"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js"
  },
  "engines": {
    "node": ">=16"
  }
}
